import React from 'react'

const RegisterPage = () => {
  return (
    <div>
        when user Login page. check user account about userName and password. if true rendering homePage, else redering RegisterPage.
    </div>
  )
}

export default RegisterPage